import { Button } from 'antd'
import React from 'react'
import { Graybtn } from './btn,style'

interface Props {
    
}

export const GrayBtn = (props: Props) => {
    return (
        <div>
            <Graybtn>
            <Button > Pack </Button>
            </Graybtn>
        </div>
    )
}
